version: '2'
services:
  postgres:
    build:
      context: ../postgres
    ports:
     - "8432:5432"
    environment:
      - POSTGRES_PASSWORD=postgres
      - AWS_ACCESS_KEY_ID=AWSAccessKeyId
      - AWS_SECRET_ACCESS_KEY=AWSSecretKey
      - WALE_S3_PREFIX=s3://bucketdockerpostgreswale
      - WALE_S3_ENDPOINT=http+path://fakes3:4569
    depends_on:
     - fakes3
  fakes3:
#    volumes:
#     - ./fakes3/:/var/data/fake-s3
    build:
      context: ../fakes3